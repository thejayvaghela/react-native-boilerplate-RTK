export enum Authentication {
  REDIRECT_LOGIN = 'redirectToLogin',
  TOKEN = 'token',
}

export enum ThemeEnums {
  LIGHT = 'light',
  DARK = 'dark',
}

export enum APIStatus {
  LOADING = 'loading',
  SUCCEEDED = 'succeeded',
  FAILED = 'failed',
}

export enum Slices {
  USER = 'user',
}
